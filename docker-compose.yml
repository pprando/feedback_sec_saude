version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: feedback_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: feedbackSaudeDomingosMartins0001
      POSTGRES_DB: feedback_db
    ports:
      - "5432:5432"